# Scale AI FDE Prep (Superseded by Folderized Airplane Pack)

Use this instead:
- `scale_fde_airplane_pack/README.md`

You asked for full per-question files (not pithy summaries), and that pack now contains separate folders and detailed `.md` for:
- coding (rich text + OA data processing)
- debugging (each bug as its own folder)
- backend practical (excluding Places API)
- inferred questions from `scale-agentex`

Requested exclusions are applied:
- excluded: poker game
- excluded: party times
- excluded: contractor + skills (already done by you)
- excluded: Google Places API practical (already done by you)

Open this first:
- `scale_fde_airplane_pack/README.md`
